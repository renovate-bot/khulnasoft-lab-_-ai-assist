pre-commit:
  parallel: true
  skip:
    - ref: main
  commands:
    lint-code:
      glob: "*.py"
      run: make lint-code LINT_WORKING_DIR={staged_files}
    lint-doc:
      glob: "*.md"
      run: make lint-doc LINT_DOC_WORKING_DIR={staged_files}
